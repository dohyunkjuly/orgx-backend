services:
  mysql:
    image: mysql:8.0
    container_name: orgx-mysql
    restart: unless-stopped
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: orgx
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: